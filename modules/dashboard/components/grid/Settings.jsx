import { Component, PropTypes } from "react";

/**
 * @class Settings
 * @classdesc
 */
export default class Settings extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>{"TEST"}</div>
    );
  }
}

Settings.propTypes = {};
