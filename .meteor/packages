# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base             # Packages every Meteor app needs to have
mobile-experience       # Packages for a great mobile UX
mongo                   # The database Meteor supports right now
jquery                  # Helpful client-side library
tracker                 # Meteor's client-side reactive programming library
session                 # Client-side reactive dictionary for your app
standard-minifiers      # JS/CSS minifiers run for production mode
es5-shim                # ECMAScript 5 compatibility for older browsers.
ecmascript              # Enable ECMAScript2015+ syntax in app code


accounts-facebook
accounts-password

semantic:ui
flemay:less-autoprefixer

kadira:flow-router
kadira:react-layout

universe:modules
#universe:collection
universe:i18n
universe:modules-npm
universe:accounts-ui
universe:utilities
universe:utilities-react
# universe:react-grid-layout    # A draggable and resizable grid layout with responsive breakpoints, for React
# universe:react-widgets
universe:react-markdown-wysiwyg # Es6 modules with react components wysiwyg based on Medium Editor, It works with markdown in<->out

#react
react-runtime

# ongoworks:bunyan-logger
#aldeed:collection2
#aldeed:simple-schema
#alanning:roles

# Reaction Commerce Packages

reactioncommerce:core
# reaction-flow-routing

# mercuria-deps
den:react-dropzone

# reaction deps

logging
reload
random
ejson
check
underscore
oauth-encryption
audit-argument-checks
spiderable

# without this FS is not defined
cfs:standard-packages@0.5.9

# do we need this?
ongoworks:bunyan-logger
